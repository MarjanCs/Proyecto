<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.quindio.sena.ejemplosqlite">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="App Registro"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Registro CodeQR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroUsuariosActivity" />
        <activity android:name=".ConsultarUsuariosActivity" />
        <activity android:name=".ConsultaComboActivity" />
        <activity android:name=".DetalleUsuarioActivity" />
        <activity android:name=".ListaPersonasRecycler" />
        <activity
            android:name=".Parametros"
            android:label="@string/title_activity_parametros"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>